-- MySQL Database Setup for Studia

-- Create database if not exists
CREATE DATABASE IF NOT EXISTS studia_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Create user if not exists (Run with appropriate privileges)
-- CREATE USER IF NOT EXISTS 'studia_user'@'localhost' IDENTIFIED BY 'studia_password';
-- GRANT ALL PRIVILEGES ON studia_db.* TO 'studia_user'@'localhost';
-- FLUSH PRIVILEGES;

USE studia_db;

-- Note: Tables will be auto-created by Hibernate when ddl-auto is set to 'update' or 'create'
-- This file is for reference and initial database setup only
